export type UserType = {
    userData: {
        name: string;
        email: string;
        password: string;
        phone: string;
    }
}

export type UserServiceType = {
    name: string;
    email: string;
    password: string;
    phone: string;
}

export type ContextTypes = {
    email: string
}